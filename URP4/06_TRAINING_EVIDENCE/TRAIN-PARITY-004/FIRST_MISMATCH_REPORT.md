# FIRST MISMATCH REPORT

No source↔adapter parity mismatch was adjudicated.

The source process exited natively with `0xC06D007F` after P0–P4 pre-fit artifacts and before the first branch checkpoint. Per contract, the adapter was not started and no retry or compatibility patch was attempted.
