# Professor Generator Notebook Intake — 2026-07-16

This directory is an immutable raw intake of two notebooks received from the professor through KakaoTalk.

Rules:

1. Preserve original filenames and bytes.
2. Do not execute, edit, clear outputs, or normalize notebook metadata in this directory.
3. The Windows read-only attribute is set on both notebook copies.
4. Static audit and derived documentation belong under `results/`, `reports/tables/`, or a bounded fork directory.
5. A new generator/formula/config must receive a new ID/version; never overwrite values produced by an older identity.

Scientific boundary:

- Intake/source delivery is confirmed.
- Notebook authorship inferred from conversation is only likely until the professor confirms it.
- Production readiness and the exact DLP VF30/VF45/VF60 configuration are unresolved.
- COM/SOUND image production in the Lattice notebook is not the RUN-139 descriptor-slicing contract.

See `source_manifest_20260716.csv` for paths, sizes, timestamps, hashes, and statuses.

