# Reproduction and resume guide — package v1.4-r2

1. Extract the ZIP to a short path such as `C:\URP4H\`.
2. Read `00_READ_ME_FIRST/README_FIRST.md` and the master ledger.
3. Verify `URP4-1_DOCTOR_HANDOFF_20260802_v1_4_r2.zip.sha256`, `URP4-1_DOCTOR_HANDOFF_20260802_v1_4_r2_INDEPENDENT_PACKAGE_QA.json` and `DELIVERY_BUNDLE_INDEX_v1_4_r2.json` before treating the archive as canonical.
4. Follow `03_CONFIG_RUNTIME_AND_COMMANDS/README_PORTABLE_REPRODUCTION.md` to recreate or bind KMK312.
5. Run only the relative read-only verifier. Do not run model generation, slicing, descriptors or Training during package verification.
6. Place the separately delivered 67-file geometry corpus according to `PORTABLE_PATH_MAP.json` and verify its delivery receipt.

Resume points:

- Direct STEP: C1 is terminal within its bounded x-only scope; no active run remains.
- Training: resume only after the native source runtime blocker is explicitly authorized for diagnosis; current submission state is blocked with evidence.
- Imported STL/F1/all58 and inverse design remain future scientific gates, not package defects.
