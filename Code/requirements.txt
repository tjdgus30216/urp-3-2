numpy>=2.0
pandas>=2.2
scipy>=1.12
scikit-image>=0.24
trimesh>=4.0
Pillow>=10.0
networkx>=3.0
opencv-python>=4.8
scikit-learn>=1.4
nbformat>=5.9
matplotlib>=3.8
# ipykernel/jupyter (or jupyterlab) is needed to actually OPEN and run the .ipynb notebooks
# in this package -- not listed here as a hard pin since most environments already have one.
# Optional for persistent homology:
# gudhi>=3.10
# Optional, only needed to import STEP/STP CAD files directly (tessellates the B-rep via
# OpenCASCADE). Without it, export STEP -> STL from your CAD tool and load the STL instead.
# cadquery>=2.4
